import React, { Component } from 'react';

class resume extends Component {
    render() {
        return (
            <div>
                Resume
            </div>
        );
    }
}

export default resume;